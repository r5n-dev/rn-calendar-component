// import fillDates from '../fillDates';

// fillDates({
//   hideExtraDays,
//   dates,
//   months,
//   monthIndex,
//   firstDay,
// })

// TODO

describe('fillDates', () => {
  describe('when firstDay is equal 0', () => {
    describe('when hideExtraDays is true', () => {
      describe('when month starts with sunday', () => {
        test.todo('TODO');
      });

      describe('when month starts with monday', () => {});
      describe('when month starts from tuesday to saturday', () => {});

      describe('when month ends with saturday', () => {});
      describe('when month ends with sunday', () => {});
      describe('when month ends from monday to friday', () => {});
    });

    describe('when hideExtraDays is false', () => {
      describe('when month starts with sunday', () => {});
      describe('when month starts with monday', () => {});
      describe('when month starts from tuesday to saturday', () => {});

      describe('when month ends with saturday', () => {});
      describe('when month ends with sunday', () => {});
      describe('when month ends from monday to friday', () => {});
    });
  });

  describe('when firstDay is equal 1', () => {
    describe('when hideExtraDays is true', () => {
      describe('when month starts with sunday', () => {});
      describe('when month starts with monday', () => {});
      describe('when month starts from tuesday to saturday', () => {});

      describe('when month ends with saturday', () => {});
      describe('when month ends with sunday', () => {});
      describe('when month ends from monday to friday', () => {});
    });

    describe('when hideExtraDays is false', () => {
      describe('when month starts with sunday', () => {});
      describe('when month starts with monday', () => {});
      describe('when month starts from tuesday to saturday', () => {});

      describe('when month ends with saturday', () => {});
      describe('when month ends with sunday', () => {});
      describe('when month ends from monday to friday', () => {});
    });
  });
});
